# Salli
personal project for an app

## Import / Export

- Import: supports delimited files (`,`, `;`, or tab) including **Wallet by BudgetBakers** CSV exports like `imports/report_*.csv`.
- Export: Settings -> Export -> use **Wallet CSV** to generate a Wallet-compatible report (semicolon-separated, Wallet column names).
