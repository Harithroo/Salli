{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome on localhost:3000",
      "url": "http://localhost:3000/index.html",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
